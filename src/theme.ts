import { MD3LightTheme as DefaultTheme } from 'react-native-paper';

export default {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: '#3f51b5',
        secondary: '#ff4081',
    },
};
